import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: #ff903c;
  font-size: 40px;
`;

export default function NavigationLayout() {
  return <Wrapper>Navigation section</Wrapper>;
}
