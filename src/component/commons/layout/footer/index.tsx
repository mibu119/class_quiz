import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 70px;
  background-color: green;
`;

export default function FooterLayout() {
  return <Wrapper>Footer section</Wrapper>;
}
